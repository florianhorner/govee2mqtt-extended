target
.*sw?
.env
docker-target
node_modules
package.json
package-lock.json
.DS_Store
.gstack/

# Keep machine-specific Conductor overrides out of version control.
/.conductor/settings.local.toml

# Agent/workspace scratch. PR proof artifacts belong in proof/ instead.
.context/

# The engineering backlog is private and lives outside the repository; ROADMAP.md is the public list.
/TODOS.md
