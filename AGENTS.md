# AGENTS.md

Agent instructions for this repo live in [`CLAUDE.md`](./CLAUDE.md): build, test and lint
commands, the Rust ≥ 1.85 toolchain requirement, project layout, and how to run the
`govee serve` bridge locally against a scratch MQTT broker.

Per-PR conventions — commit message format, proof blocks, bypass policy — are in
[`CONTRIBUTING.md`](./CONTRIBUTING.md).

Both files are self-sufficient for agents that only see repo-local files. Nothing is
duplicated here, so there is nothing here to drift.
